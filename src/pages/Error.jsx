

function Error({ type }) {
    switch(type){
    case page:
        return(
            <div>
                
            </div>
        )

    case server:
        return(
            <div>

            </div>
        )
    }
}

export default Error